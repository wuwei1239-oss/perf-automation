# 飞书自动考核系统 — 项目总结

## 一、项目定位

基于飞书多维表格的**绩效考核自动化系统**，替代原有的 Excel 手工计算流程。组长在手机/电脑上填报工单 → 自动写入飞书流水账 → 自动计算绩效 → 写入个人绩效明细表 + 更新考核汇总表。

---

## 二、核心文件与职责

| 文件 | 职责 |
|------|------|
| `feishu_client.py` | 飞书多维表格 API 客户端，封装 token 管理、CRUD、批量操作 |
| `perf_engine.py` | 绩效计算引擎，接收工序/人员/批次/加减分参数，输出每人每批次的得分明细 |
| `app_feishu_flow.py` | **流水账驱动模式**：从飞书流水账读取已有记录 → 批量计算绩效 → 写入明细表 + 汇总表 (`--dry-run` 预览, `--no-clear` 不清旧数据) |
| `app_feishu_form.py` | **Web 表单模式**：Flask 网页，手机上填完提交 → 直接写流水账 + 自动计算绩效，一步到位 |
| `rules_excel.py` | 从 Excel 规则库提取品种/工序规则 |
| `transform.py` / `write_v5.py` | 旧版 Excel 工具，辅助数据转换 |
| `_verify_*.py` 系列 | WPS API 探索脚本（对接阶段产物，非主流程） |
| `app.py` / `app_feishu.py` | 早期版本的入口文件 |
| `Procfile` | Railway/Render 部署配置 |
| `requirements.txt` | Python 依赖 |

---

## 三、飞书多维表格结构

| 表名 | table_id | 作用 |
|------|----------|------|
| 品种工序规则 | tbl17rYHOsFHuq8D | 21 个品种的工序基础分配置 |
| 系数规则 | tbl4Mh9i1fbpYmmt | 14 条角色系数（代理组长 1.1 等） |
| 车间人员 | tbl1AdIv8VJAGkwo | 绩效考核表引用的 14 名人员基准 |
| 人员工序绑定表 | tblrqjKQTkE3XEID | 人员与工序的关联关系 |
| **每日报工流水账** | tblkDy41lNZC2Z9B | 📥 输入：组长填报的工单（33 字段） |
| **个人绩效明细表** | tblSSKwtVTK4fCom | 📤 输出：每人每批次得分明细（28 字段） |
| **绩效考核表** | tbl8Qg5pvMYQkO5V | 📤 输出：人员月度汇总（月累计等） |

---

## 四、计算流程

```
┌─────────────────┐
│ 组长填报工单     │  (飞书表单 / Web 表单)
└────────┬────────┘
         ▼
┌─────────────────┐
│ 每日报工流水账   │  工序、人员、批次、工时、大清/缺员/质量等
└────────┬────────┘
         ▼
┌─────────────────┐
│ perf_engine.py  │  品种规则 + 系数规则 → 计算每人得分
│   calc_perf()   │  产出：系数分、加减分、总分、备注
└────────┬────────┘
         ▼
┌─────────────────┐
│ 个人绩效明细表   │  记录每条得分明细（关联流水账记录）
└────────┬────────┘
         ▼
┌─────────────────┐
│ 绩效考核表       │  按人名汇总本月累计得分
└─────────────────┘
```

两种触发方式：
- **批量模式** (`app_feishu_flow.py`)：从流水账拉取所有记录，批量计算，适合定期结算
- **实时模式** (`app_feishu_form.py`)：Web 表单提交后即时计算，适合日常报工

---

## 五、测试进度

| 测试项 | 状态 | 说明 |
|--------|------|------|
| 飞书 API 连接与 Token | ✅ 通过 | 可正常获取 token，CRUD 操作正常 |
| 规则读取（品种规则、系数规则、人员列表） | ✅ 通过 | 21 品种 + 14 系数 + 14 人员 |
| `perf_engine` 计算逻辑 | ✅ 通过 | 计算正确（压片/配料/外包等工序验证过） |
| 流水账写入 | ✅ 通过 | Web 表单提交，33 个字段正确映射写入 |
| 绩效明细写入 | ✅ 通过 | 2 人压片测试，明细正确写入 |
| 关联流水账字段 | ✅ 通过 | 明细表 `关联流水账` 字段正确关联 |
| 汇总表更新 | ⚠️ 部分通过 | 仅对 `绩效考核表` 中已有的人员更新；`app_feishu_flow.py` 批量模式已验证通过 |
| Web 表单端到端 | ✅ 通过 | 提交 → 流水账 → 计算 → 明细写入，全链路跑通 |
| 手机浏览器适配 | ✅ 通过 | 响应式设计，viewport 适配 |
| 清理测试数据 | ✅ 完成 | E2E 测试数据已删除 |

---

## 六、待完成事项

1. **部署上线**：目前 Flask 在本机运行，需部署到 Railway/Render 获得公网 URL（`Procfile` 已配置好）
2. **汇总表更新完善**：`app_feishu_form.py` 中的汇总更新仅匹配已有人员，对于新人需考虑是否自动添加到绩效考核表
3. **报工模式细化**：目前默认 "正常生产"，"只报大清" 模式的计算逻辑需确认与 perf_engine 的配合
4. **前端增强**：工序切换时动态显示/隐藏相关加减分字段（如压片才显示冲型选择）
5. **错误处理增强**：飞书 API 限流、超时重试、表单验证细化
6. **多批次支持**：`batch_count > 1` 时多批次的 box 分配策略需明确
7. **WPS API 对接**：探索过但未集成，后续如果切换到 WPS 表格可复用 API 结构

---

## 七、运行方式

```bash
# 批量模式（从流水账批量结算）
python app_feishu_flow.py              # 正常计算 + 写入
python app_feishu_flow.py --dry-run    # 预览模式
python app_feishu_flow.py --no-clear   # 追加不清旧

# Web 表单模式（日常报工）
python app_feishu_form.py              # 启动 Flask → http://127.0.0.1:5000

# 健康检查
curl http://127.0.0.1:5000/health
```

---

## 八、技术栈

- **Python 3** + Flask（Web 表单）
- **飞书开放平台 API** — 多维表格 Bitable（tenant_access_token 认证）
- **perf_engine** — 基于品种工序规则的加权计分引擎
- **HTML/CSS/JS** — 响应式移动端表单（无外部框架依赖）
- **部署目标** — Railway / Render（Procfile 已配置）